# Required
JWT_SECRET=your-super-secret
ADMIN_USER=admin
ADMIN_PASS=change-me

# Database (if not set, uses SQLite file)
# DATABASE_URL=postgresql+psycopg2://<user>:<pass>@<host>:5432/<db>

# ISO8583 live TCP (optional; if not set, local ACKs are used)
# ISO_HOST=your-iso-host
# ISO_PORT=5000

# Optional
JWT_TTL_MIN=30
MAC_HEX_KEY=00112233445566778899AABBCCDDEEFF
